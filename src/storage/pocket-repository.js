// This mutable map will be used as storage.
const urlMap = new Map()

const PocketRepository = {
  // return either the URL or null
  getUrl(hash) {

  },
  // no return
  saveAssociation(hash, url) {

  }
}

module.exports = PocketRepository
